on_startup = {
	if = {
		limit = { tag = OBS }
		save_global_event_target_as = Global
		3204 = {
			save_global_event_target_as = City
			set_province_flag = daily
		}
	}

	if = {
		limit = { NOT = { has_country_flag = set_up_cookies } }
		init_cookies = yes
		set_country_flag = set_up_cookies
	}
}
