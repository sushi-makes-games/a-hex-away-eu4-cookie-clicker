# # GLOBAL = { on_global_daily = yes }
on_global_daily = {
	every_province = {
		limit = { is_city = yes }
		hidden_effect = { variable_effect = { which = passive_rank effect = simulate_daily_click } }
	}
}

# # GLOBAL = { on_global_monthly = yes }
on_global_monthly = {
	every_province = {
		limit = { is_city = yes }
		hidden_effect = { variable_effect = { which = sponsor_rank effect = simulate_monthly_click } }
	}
}